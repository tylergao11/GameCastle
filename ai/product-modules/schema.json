{
  "schemaVersion": 1,
  "description": "Product modules are game-factory building blocks. They are the single source of truth for module compilation, networking, safe LLM1 capability summaries, and runtime ownership. Capabilities are declared inline and derived from here; there is no separate capabilities directory.",
  "requiredFields": [
    "schemaVersion",
    "id",
    "name",
    "summary",
    "llm1Card",
    "category",
    "defaultPreset",
    "presets",
    "repositoryPolicy",
    "revision",
    "semanticContract",
    "spatialContract",
    "declarationContract",
    "lifecycleContract",
    "ownershipContract",
    "acceptanceContract",
    "networking",
    "compiler",
    "capabilities"
  ],
  "categories": [
    "core",
    "shell",
    "system",
    "meta",
    "network"
  ],
  "fieldNotes": {
    "llm1Card": "Short product-level card safe for LLM1. Do not include internal target instructions.",
    "capabilities": "Array of atomic capability cards embodied by this module. Each card has id, name, summary, llm1Hint, provides, requires, constraints, and networking. These cards only feed safe LLM1 creative summaries and capability checks; they must not carry internal target commands, examples, or LLM2 repair context. Cards are deduplicated by id across all modules.",
    "compiler.targetPlan": "Internal target template lines consumed by ai/pipeline.js.",
    "compiler.slots": "Internal module-link slots. Slots are filled by compiler.links and are not automatically LLM2-configurable.",
    "compiler.slotUpdateTemplates": "Internal update templates used when a newly installed module links into an already installed base module.",
    "compiler.links": "Module-level integration hooks that fill named slots in other modules.",
    "compiler.configureUpdateTemplates": "Per-parameter update templates for configuring an already installed module from ProjectWorld state.",
    "interaction": "AI-visible fixed runtime interaction contract. Use this for trigger/copy constraints without exposing internal target templates.",
    "repositoryPolicy": "Module repository strategy. Product modules are reused from the module repository; expensive generated module variants must be promoted back as cloud-module candidates instead of remaining one-off outputs.",
    "revision": "Immutable approved local revision. Planner pins this value and manifest hash in every composition plan.",
    "semanticContract": "Canonical semantic refs provided and required by this module. This is the only Planner capability mapping.",
    "spatialContract": "Supported topology and abstract spatial role requirements; never final coordinates.",
    "declarationContract": "Prototype, bounds, layer and ownership declarations consumed before PlacementResolver.",
    "lifecycleContract": "Provided or required start, pause, failure, restart and continue surfaces.",
    "ownershipContract": "Stable artifact ids and cleanup/state policy for safe configure, remove and replace.",
    "acceptanceContract": "Create, continue, failure and layout fixture ids for evidence.",
    "networking": "Declares what networking models this module supports. Used by compiler to match with network modules."
  }
}
