var assert=require('assert').strict, path=require('path'), pipeline=require('./pipeline'), compiler=require('./module-compiler');
async function main(){var catalog=compiler.loadProductModuleCatalog(path.join(__dirname,'product-modules'));var plan={planId:'idle.single',catalogFingerprint:'test',determinism:{outputHash:'test'},operations:[{op:'install',parameters:{},toModule:{moduleId:'core.idle_clicker',revision:'local-v1',manifestHash:'test'}}]};var compiled=compiler.compileCompositionPlan(plan,catalog),project=pipeline.emptyProject('idle'),ops=pipeline.parseTargetPlan(compiled.targetPlanText);for(var i=0;i<ops.length;i++)assert((await pipeline.execute(project,ops[i])).ok);assert(project.layouts[0].objects.some(function(o){return o.name==='Clicker';}));assert(JSON.stringify(project.layouts[0].events).indexOf('Production')>=0);console.log('[IdleClickerFixture] click, passive growth and upgrade events passed');}
main().catch(function(e){console.error(e);process.exit(1);});
