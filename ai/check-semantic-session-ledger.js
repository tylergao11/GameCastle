var assert = require('assert');
var dictionary = require('./capability-semantic-dictionary');
var ledgerApi = require('./semantic-session-ledger');
var sourceContract = require('./game-semantic-source');
var prompt = require('./semantic-llm2-prompt');
var referenceRuntime = require('./semantic-reference-runtime');
var draftApi = require('./semantic-draft');
var runLedgerApi = require('./semantic-run-ledger');
var commanderContext = require('./semantic-commander-context');
var index = dictionary.buildIndex();
var source = { schemaVersion: 4, documentKind: 'game-semantic-source', dictionarySource: index.source, game: { semanticId: 'demo', name: 'Demo' }, entities: [{ semanticId: 'player', roles: ['player'], objectTypeRef: 'gdjs://object/Sprite::Sprite', behaviorTypeRefs: [], members: [{ semanticId: 'jump_height', roles: ['movement'], value: 100, bindings: [] }] }], events: [], assetIntents: [], layoutIntents: [], tuningPolicies: { relativeChange: { slight: { mode: 'percentage', value: 0.1 } } } };
var ledger = ledgerApi.create(source, { index: index, worldVersion: 1 });
var revision = { schemaVersion: 4, documentKind: 'game-semantic-revision', baseSourceHash: sourceContract.sourceHash(source), operations: [{ op: 'adjust_member_value', target: { entity: 'player', member: 'jump_height' }, direction: 'increase', degree: 'slight' }] };
var committed = ledgerApi.commit(ledger, source, revision, { index: index, worldVersion: 2 });
assert.strictEqual(committed.entry.structuralDiff.collections.entities.changed.length, 0, 'numeric tuning must not create a world structure diff');
var baseline = ledgerApi.context(committed.ledger, committed.source, { index: index, worldVersion: 2 });
assert.strictEqual(baseline.mode, 'baseline');
assert.strictEqual(baseline.world.payload.entities[0].members[0].value, undefined, 'baseline world must omit concrete values');
var context = ledgerApi.context(committed.ledger, committed.source, { index: index, worldVersion: 2, knownStructureHash: committed.ledger.latest.structureHash });
assert.strictEqual(context.mode, 'no-structure-change');
var references = referenceRuntime.create(index);
var draft = draftApi.create(references, committed.source);
var runLedger = runLedgerApi.create('jump a little higher');
var llmContext = commanderContext.build(references, draft, 'jump a little higher', 'precise responsive movement', [], runLedger);
var userPrompt = prompt.buildUserPrompt({ userRequest: 'jump a little higher', creativeVision: 'precise responsive movement', context: llmContext, feedbackBatch: null });
assert.strictEqual(userPrompt.indexOf('"value":110'), -1, 'prompt world context must not disclose current values');
assert.strictEqual(/gdjs:\/\//.test(userPrompt), false, 'prompt context must expose runtime handles rather than internal references');
var systemPrompt = prompt.buildSystemPrompt();
['GameCastle Semantic Commander', 'Role', 'Output', 'WORLD', 'Slot Meanings', 'Foundation Operations', 'Extension Reads', 'DSL', 'Batch Shape', 'Values'].forEach(function(section) {
  assert(systemPrompt.indexOf(section) >= 0, 'LLM2 prompt must retain Comdr-style fill-in section: ' + section);
});
assert(systemPrompt.indexOf('Design values come from [task] and [creative-vision]') >= 0, 'LLM2 must be allowed to write design values for a new source');
assert.strictEqual(/search_semantic_members|list_object_types|semantic-context-request|semantic-parameter-request/.test(systemPrompt), false, 'retired query and JSON request protocols stay absent from the prompt');
assert(!/\bnever\b|don't|\btemplate\b|\bexample\b/i.test(systemPrompt), 'LLM2 prompt must use positive fill-in guidance without template cases or prohibitive phrasing');
assert(userPrompt.indexOf('condition|input.key.just-pressed|') >= 0, 'foundation event algebra must be task-independent prompt context');
assert.strictEqual(/\bc\d+\|/.test(userPrompt), false, 'foundation prompt must not expose raw capability handles');
console.log('[SemanticSessionLedger] no-value baseline, structural diff, and event-algebra prompt policy passed');
