var assert = require('assert'); var state = require('./asset-animation-state-machine'); var machine = state.createAnimationStateMachine();
assert.equal(machine.framePolicy.defaultFramesPerState, 1); assert.equal(machine.framePolicy.maxGeneratedKeyframesPerAction, 2); assert.equal(state.nextState(machine, 'idle', 'move'), 'move'); assert.equal(state.nextState(machine, 'move', 'hit'), 'hit'); assert.equal(state.nextState(machine, 'hit', 'recover'), 'idle'); assert.equal(state.nextState(machine, 'idle', 'die'), 'death'); console.log('[AssetAnimationStateMachine] low-frame intent state machine passed');
