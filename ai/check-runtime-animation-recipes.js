var assert = require('assert'); var animation = require('./runtime-animation-recipes');
var binding = animation.bindSpriteAsset({ assetId: 'local.ghost', styleTags: ['gamecastle.style-1'] }, { subject: 'ghost', motion: 'float' });
assert.equal(binding.owner, 'RuntimeLinker'); assert.equal(binding.styleId, 'gamecastle.style-1'); assert(binding.animation.states.idle.length > 0); assert(binding.animation.states.hit.length > 0); assert.equal(binding.anchor, 'bottom-center'); assert.equal(binding.stateMachine.framePolicy.defaultFramesPerState, 1); assert.equal(animation.bindSpriteAsset({ assetId: 'tagged.asset', styleTags: ['arcade'] }).styleId, 'gamecastle.style-1'); assert.throws(function() { animation.bindSpriteAsset({ assetId: 'bad.style', styleId: 'missing.style' }); }, /Unknown asset style/); console.log('[RuntimeAnimationRecipes] VisualIntent to playable sprite states passed');
