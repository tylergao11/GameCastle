var crypto = require('crypto');
var fs = require('fs');
var path = require('path');
var derivation = require('./asset-derivation-pipeline');
var frameSet = require('./frame-set');
var validator = require('./asset-production-contract-validator');

function clone(value) { return value === undefined ? undefined : JSON.parse(JSON.stringify(value)); }
function hash(value) { return crypto.createHash('sha256').update(Buffer.isBuffer(value) ? value : JSON.stringify(value)).digest('hex'); }
function fail(code, message, owner) { var error = new Error(message); error.code = code; error.owner = owner || 'AssetProductionPipeline'; throw error; }
function verifyFile(file, expectedHash) { if (!file || !fs.existsSync(file)) fail('ASSET_REVISION_MISSING', 'Accepted asset file is unavailable.'); var bytes = fs.readFileSync(file), actual = hash(bytes); if (expectedHash && actual !== expectedHash) fail('ASSET_REVISION_HASH_MISMATCH', 'Accepted asset file hash changed.'); return actual; }
function revisionId(candidate) { return candidate.revisionId || candidate.assetId || 'asset-revision.' + candidate.sha256; }
function loopState(workItem, phase, currentRevisionId) { return { loopId: 'production.' + workItem.workItemPlanId, workItemPlanId: workItem.workItemPlanId, currentRevisionId: currentRevisionId || null, phase: phase, attempt: phase === 'accepted' ? 1 : 0, budgets: clone(workItem.retryBudget || { generation: 1 }), observationReceiptIds: [], pendingAction: phase === 'debt' ? 'RETRY_PRODUCTION' : null, historyHash: hash([workItem.workItemPlanId, phase, currentRevisionId || null]) }; }
function acceptance(workItem, candidate) { var id = frameSet.isFrameSet(candidate) ? candidate.revisionId : revisionId(candidate), evidence = frameSet.isFrameSet(candidate) ? [candidate.contentHash, candidate.acceptanceReceiptId] : (candidate.derivationReceipts || []).map(function(item) { return item.outputHash; }).concat([candidate.sha256]); var value = { workItemPlanId: workItem.workItemPlanId, finalRevisionId: id, targetVisualSlotId: workItem.targetVisualSlotId, deterministicEvidenceIds: evidence, styleId: workItem.assetSpec.styleId, decision: 'accepted' }; validator.validateArtifact('WorkItemAcceptanceReceipt', value); return value; }
function acceptedResult(workItem, candidate, masterImage) { var current = frameSet.isFrameSet(candidate) ? candidate : Object.assign({}, candidate, { revisionId: revisionId(candidate), resourceKind: candidate.resourceKind || workItem.assetSpec.resourceKind || 'image', format: candidate.format || path.extname(candidate.path).slice(1).toLowerCase(), status: 'accepted' }); return { workItem: workItem, accepted: true, candidate: Object.assign({}, candidate, { revisionId: current.revisionId }), currentRevision: current, masterImage: masterImage || null, acceptanceReceipt: acceptance(workItem, current), debt: null, loopState: loopState(workItem, 'accepted', current.revisionId) }; }
function validateResolved(workItem, candidate) {
  if (frameSet.isFrameSet(candidate)) { var accepted = frameSet.validate(candidate); accepted.frames.forEach(function(frame) { verifyFile(frame.path, frame.sha256); }); return Object.assign({}, accepted, { source: candidate.source || 'assetLibrary' }); }
  if (!candidate || !candidate.path) fail('ASSET_PRODUCTION_CANDIDATE_INVALID', 'Resolved asset is incomplete.');
  var actual = verifyFile(candidate.path, candidate.sha256), format = String(candidate.format || path.extname(candidate.path).slice(1)).toLowerCase();
  if ((workItem.assetSpec.acceptedFormats || []).indexOf(format) < 0) fail('ASSET_PRODUCTION_FORMAT_INVALID', 'Resolved asset format is not accepted by the semantic requirement.');
  return Object.assign({}, candidate, { sha256: actual, format: format, source: candidate.source || 'localExplicit' });
}
async function createFromMaster(input) {
  var ports = input.ports || {}; if (typeof ports.generateMaster !== 'function') fail('MASTER_IMAGE_PROVIDER_UNAVAILABLE', 'Asset creation requires a master-image provider.', 'ComfyUIMasterImageProvider');
  var state = { runId: input.runId, projectId: input.projectId, slot: input.workItem.assetSpec, workItem: input.workItem, projectAssetDir: input.projectAssetDir }, master = await ports.generateMaster(state);
  if (master && master.assetBlobRef) { if (typeof ports.materializeCandidate !== 'function') fail('MASTER_IMAGE_MATERIALIZATION_UNAVAILABLE', 'Master image provider did not expose materialization.'); master = await ports.materializeCandidate(Object.assign({}, state, { candidate: master })); }
  if (!master || !master.path) fail('MASTER_IMAGE_INVALID', 'Master image provider returned no materialized PNG.');
  var inspected = derivation.inspectFile(master.path, master.sha256), masterRevision = Object.assign({}, master, { revisionId: master.revisionId || 'master-image.' + inspected.sha256, sha256: inspected.sha256, width: inspected.raster.width, height: inspected.raster.height, providerReceipt: master.providerReceipt || { receiptId: 'provider.' + inspected.sha256.slice(0, 24) } });
  validator.validateArtifact('MasterImageRevision', masterRevision);
  var derivationInput = { master: masterRevision, slot: input.workItem.assetSpec, projectAssetDir: input.projectAssetDir, backgroundRemoval: ports.backgroundRemoval };
  var candidate = input.workItem.artifactKind === 'frame-set' ? await derivation.deriveFrameSet(derivationInput) : await derivation.deriveStatic(derivationInput);
  return acceptedResult(input.workItem, Object.assign({}, candidate, { source: input.workItem.artifactKind === 'frame-set' ? 'frameSetProduction' : 'deterministicDerivation' }), masterRevision);
}
async function runWorkItem(input) { try { if (input.candidate) return acceptedResult(input.workItem, validateResolved(input.workItem, input.candidate), null); if ((input.workItem.assetSpec.resourceKind || 'image') !== 'image') fail('ASSET_PRODUCTION_EXTERNAL_RESOURCE_REQUIRED', 'Non-image semantic assets require an explicit accepted local resource.'); return await createFromMaster(input); } catch (error) { return { workItem: input.workItem, accepted: false, candidate: null, currentRevision: null, masterImage: null, acceptanceReceipt: null, debt: { slotId: input.workItem.slotId, code: error.code || 'ASSET_PRODUCTION_FAILED', owner: error.owner || 'AssetProductionPipeline', message: error.message, recoveryPhase: 'master-image-or-derivation' }, loopState: loopState(input.workItem, 'debt', null) }; } }
function restoredUsable(result) { try { if (!result || !result.accepted || !result.candidate) return false; if (frameSet.isFrameSet(result.candidate)) { frameSet.validate(result.candidate).frames.forEach(function(frame) { verifyFile(frame.path, frame.sha256); }); return true; } verifyFile(result.candidate.path, result.candidate.sha256); return true; } catch (_error) { return false; } }
function readLedger(file, plan) { if (!file || !fs.existsSync(file)) return { schemaVersion: 2, productionSetId: plan.productionSetId, planContentHash: plan.contentHash, workItems: {} }; try { var value = JSON.parse(fs.readFileSync(file, 'utf8')); if (value.productionSetId === plan.productionSetId && value.planContentHash === plan.contentHash) return value; } catch (_error) {} return { schemaVersion: 2, productionSetId: plan.productionSetId, planContentHash: plan.contentHash, workItems: {} }; }
function writeLedger(file, ledger) { if (!file) return; fs.mkdirSync(path.dirname(path.resolve(file)), { recursive: true }); fs.writeFileSync(file, JSON.stringify(ledger, null, 2)); }
async function runProductionSet(input) {
  validator.validatePlan(input.plan); var ledger = readLedger(input.ledgerPath, input.plan), results = [];
  for (var index = 0; index < input.plan.workItems.length; index++) { var workItem = input.plan.workItems[index], restored = ledger.workItems[workItem.workItemPlanId], result = restoredUsable(restored) ? restored : await runWorkItem({ runId: input.runId, projectId: input.projectId, workItem: workItem, ports: input.ports, candidate: (input.candidates || {})[workItem.slotId], projectAssetDir: input.projectAssetDir }); results.push(result); ledger.workItems[workItem.workItemPlanId] = result; writeLedger(input.ledgerPath, ledger); }
  var accepted = results.filter(function(result) { return result.accepted; }), expected = input.plan.coveragePolicy.requiredTargetVisualSlotIds.slice().sort(), actual = accepted.map(function(result) { return result.workItem.targetVisualSlotId; }).sort(), complete = JSON.stringify(expected) === JSON.stringify(actual), byTarget = {}; accepted.forEach(function(result) { byTarget[result.workItem.targetVisualSlotId] = result.currentRevision.revisionId; });
  var receipt = { productionSetId: input.plan.productionSetId, workItemAcceptanceReceiptIds: accepted.map(function(result) { return 'work-acceptance.' + hash(result.acceptanceReceipt).slice(0, 24); }), requiredSlotCoverage: { expectedTargetVisualSlotIds: expected, acceptedTargetVisualSlotIds: actual, missingTargetVisualSlotIds: expected.filter(function(target) { return actual.indexOf(target) < 0; }), complete: complete }, acceptedRevisionByTargetVisualSlotId: byTarget, decision: complete ? 'accepted' : 'debt' }; validator.validateSetAcceptance(receipt);
  return { plan: input.plan, workItems: results, acceptanceReceipt: receipt, pass: complete, decision: complete ? 'pass' : 'blocked', debts: results.filter(function(result) { return result.debt; }).map(function(result) { return result.debt; }) };
}

module.exports = { runWorkItem: runWorkItem, runProductionSet: runProductionSet };
