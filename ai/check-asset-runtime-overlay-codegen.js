var assert = require('assert');
var code = require('./asset-runtime-overlay-codegen').generate({ bindings: [{ binding: 'asset.hero', asset: { path: 'assets/generated/hero.png' } }] });
assert(code.indexOf('project.resources.resources.push') >= 0);
assert(code.indexOf('resourceName(binding.binding, image)') >= 0);
assert(code.indexOf('file:image') >= 0);
assert(code.indexOf('layout.usedResources.push({ name:resource })') >= 0);
assert(code.indexOf('type:"Sprite"') >= 0);
assert(code.indexOf('layout.instances.push') >= 0);
assert(code.indexOf('GameCastleAssetRuntime') >= 0);
assert(code.indexOf('document.createElement') < 0);
console.log('[AssetRuntimeOverlay] GDevelop Sprite injection code passed');
